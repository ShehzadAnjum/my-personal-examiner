/**
 * Auth Configuration for Better Auth CLI
 *
 * This file is used by the Better Auth CLI to manage database migrations.
 * It re-exports the auth instance from the core server configuration.
 */
export { auth } from "@/lib/auth/core/server";
